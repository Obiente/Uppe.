---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`text-center py-12 ${className}`}>
  <div class="bg-status-success/20 p-6 rounded-2xl w-20 h-20 mx-auto mb-6 flex items-center justify-center">
    <svg class="w-10 h-10 text-status-success" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
    </svg>
  </div>
  <h3 class="text-primary font-semibold text-lg mb-2">No Recent Alerts</h3>
  <p class="text-secondary">All systems are running smoothly</p>
  <p class="text-status-success text-sm mt-2 font-medium">✨ Everything looks great!</p>
</div>
