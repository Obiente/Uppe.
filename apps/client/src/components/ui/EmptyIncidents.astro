---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;

const containerClasses = `text-center py-12 ${className || ''}`;
const iconContainerClasses = 'bg-status-success/20 p-6 rounded-2xl w-20 h-20 mx-auto mb-6 flex items-center justify-center';
const iconClasses = 'w-10 h-10 text-status-success';
const titleClasses = 'text-primary font-semibold text-lg mb-2';
const messageClasses = 'text-secondary';
const statusClasses = 'text-status-success text-sm mt-2 font-medium';
---

<div class={containerClasses}>
	<div class={iconContainerClasses}>
		<svg class={iconClasses} fill="none" stroke="currentColor" viewBox="0 0 24 24">
			<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
		</svg>
	</div>
	<h3 class={titleClasses}>No Recent Incidents</h3>
	<p class={messageClasses}>No incidents reported in the last 24 hours</p>
	<p class={statusClasses}>🎉 All systems are operational</p>
</div>
