---
import Button from './Button.astro';

interface Props {
  lastCheck: string;
  class?: string;
}

const { lastCheck, class: className = '' } = Astro.props;
---

<div class={`mt-4 pt-4 border-t border-primary ${className}`}>
  <div class="flex items-center justify-between text-xs text-secondary">
    <span>Last checked: {lastCheck}</span>
    <Button variant="secondary" size="sm">
      View Details →
    </Button>
  </div>
</div>
