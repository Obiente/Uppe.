{
  "name": "@uppe/service",
  "scripts": {
    "dev": "node ../../scripts/dev.mjs service",
    "add": "cargo add",
    "build": "cargo build --release",
    "build:debug": "cargo build",
    "format": "rustfmt **/**/*.rs"
  }
}
