{
  "name": "@uppe/service",
  "scripts": {
    "dev": "cargo run dev",
    "add": "cargo add",
    "build": "cargo build --release",
    "build:debug": "cargo build",
    "format": "rustfmt **/**/*.rs"
  }
}
