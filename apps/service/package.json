{
  "name": "@uppe/service",
  "scripts": {
    "dev": "cargo run",
    "add": "cargo add",
    "build": "cargo build --release",
    "build:debug": "cargo build",
    "format": "rustfmt **/**/*.rs"
  }
}
